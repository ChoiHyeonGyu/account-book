<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="notice">

	<select id="getList" resultType="noticedvo">
		<![CDATA[
			select content as noticeContent, title as noticeTitle, day as noticeDay, hit as noticeHit from notice
		]]>
	</select>
	
	<select id="getList2" resultType="noticedvo">
		<![CDATA[
			select content as qnaContent, title as qnaTitle, day as qnaDay, hit as qnaHit from qna
		]]>
	</select>
		
</mapper>